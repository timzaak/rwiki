[project]
name = "eval"
version = "0.1.0"
description = "RAG evaluation pipeline for RWiki"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31",
    "pandas>=2.0",
    "ragas>=0.2",
    "datasets>=2.14",
    "openai>=1.0",
]
